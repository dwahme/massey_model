
class City:

    def __init__(self, groups, neighborhoods, dissimilarity_index):
        self.groups = groups
        self.neighborhoods = neighborhoods
        self.dissimilarity_index = dissimilarity_index

        self.matrix = self.generate()

    def generate(self):
        matrix = []

        return matrix
